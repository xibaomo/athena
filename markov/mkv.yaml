MARKOV:
    PROB_CAL_TYPE: 2 # 0 - open price track, 1 - transmat, 2 - solve eqn
    NUM_PARTITIONS: 601 # number of partitions between tp & sl returns. Only for PROB_CAL_TYPE=2

#----------- modifiable parameters -------------
    BUY_ONLY: 0
    LOOKBACK: 300
    TP_RETURN:  0.005
    SL_RETURN:  auto  # 'auto' gives -TP_RETURN

    POS_PROB_THRESHOLD: 0.65

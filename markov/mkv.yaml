MARKOV:
    PROB_CAL_TYPE: 3 # 0 - open price track, 1 - transmat, 2 - solve eqn, 3 - first hit prob
    NUM_PARTITIONS: 601 # number of partitions between tp & sl returns. Only for PROB_CAL_TYPE=2,3

#----------- modifiable parameters -------------
    LOOKBACK: 60
    LONG_LOOKBACK: 1000
    UB_RETURN:  0.005
    LB_RETURN:  auto  # 'auto' gives -UB_RETURN
    STEPS: 100

    OVERBUY_THD: 0.75
    OVERSELL_THD: 0.15

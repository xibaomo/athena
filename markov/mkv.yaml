MARKOV:
    LOOKBACK: 1000
    PROB_CAL_TYPE: 1 # 0 - open price track, 1 - transmat
    NUM_STATES: 3 # number of states between tp & sl. Works only when PROB_CAL_TYPE=1

    TP_RETURN:  0.003
    SL_RETURN:  -0.002   # 'auto' gives -TP_RETURN

    POS_PROB_THRESHOLD: 0.6
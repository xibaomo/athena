MARKOV:
    LOOKBACK: 2000
    PROB_CAL_TYPE: 2 # 0 - open price track, 1 - transmat, 2 - solve eqn
    NUM_STATES: 7 # number of states between tp & sl. Works only when PROB_CAL_TYPE=1

    NUM_PARTITIONS: 500 # number of partitions between tp & sl returns. Only for PROB_CAL_TYPE=2

    TP_RETURN:  0.004
    SL_RETURN:  -0.004  # 'auto' gives -TP_RETURN

    POS_PROB_THRESHOLD: 0.6